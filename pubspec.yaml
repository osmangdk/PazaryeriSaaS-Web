name: frontend
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  dio: ^5.4.3+1
  shared_preferences: ^2.2.3
  go_router: ^14.1.4
  google_fonts: ^8.2.1
  url_launcher: ^6.3.0
  image_picker: ^1.1.2
  # Barkod / QR okuyucu
  mobile_scanner: ^5.2.3
  # Biyometrik giriş (Parmak İzi / Face ID)
  local_auth: ^2.3.0
  # Push bildirimler
  firebase_core: ^3.6.0
  firebase_messaging: ^15.1.3
  flutter_local_notifications: ^17.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/

